{
	"layer": "top",
	"position": "top",
	"height": 32, 
	"spacing": 0, // Handled beautifully by individual CSS margins instead

	"modules-left": [
		"custom/media",
		"hyprland/window"
	],
	"modules-center": [
		"hyprland/workspaces"
	],
	"modules-right": [
		"pulseaudio",
		"bluetooth",
		"network",
		"clock"
	],

	"hyprland/window": {
		"format": "󱂬 {title}",
		"max-length": 30,
		"separate-outputs": true,
		"rewrite": {
			"": "Desktop" // Displays clean placeholder text when no window is open
		}
	},
	"custom/media": {
		"format": "󰝚 {}",
		"max-length": 35,
		"exec": "playerctl metadata --format '{{title}} - {{artist}}' 2> /dev/null",
		"on-click": "playerctl play-pause",
		"interval": 2
	},
	"hyprland/workspaces": {
		"format": "{name}",
		"persistent-workspaces": {
			"DP-4": [1, 2, 3, 4, 5],
			"HDMI-A-2": [6, 7, 8, 9, 10]
		}
	},
	"network": {
		"format-wifi": "󰤨 {essid}",
		"format-ethernet": "󰈀 {ipaddr}/{cidr}",
		"format-disconnected": "󰤮 Disconnected",
		"on-click": "kitty --class dotfiles-floating -e nmtui"
	},
	"bluetooth": {
		"format": "󰂯",
		"format-on": "󰂯",
		"format-off": "󰂲",
		"format-disabled": "󰂲",
		"on-click": "blueman-manager"
	},
	"pulseaudio": {
		"format": "{icon} {volume}%",
		"format-muted": "󰝟 Muted",
		"format-icons": {
			"default": ["󰕿", "󰖀", "󰕾"]
		},
		"scroll-step": 2,
		"on-click": "pavucontrol"
	},
	"clock": {
		"interval": 60,
		"format": "󱑒 {:L%a %d %b %H:%M}"
	}
}
